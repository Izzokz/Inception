ALTER USER 'root'@'localhost' IDENTIFIED BY '${DB_PW_MOD}';
CREATE DATABASE IF NOT EXISTS ${DB_NAME};
CREATE USER IF NOT EXISTS '${DB_USR}'@'%' IDENTIFIED BY '${DB_PW}';
GRANT ALL PRIVILEGES ON ${DB_NAME}.* TO '${DB_USR}'@'%';
FLUSH PRIVILEGES;
